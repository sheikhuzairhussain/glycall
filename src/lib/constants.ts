/**
 * Shared constants used across the application
 */

/**
 * Resource ID used to identify the chat resource in memory storage.
 * All threads and messages are associated with this resource.
 */
export const RESOURCE_ID = "glyphic-chat";

/**
 * Default thread ID used when no thread is specified.
 * TODO: Replace with proper user authentication
 */
export const DEFAULT_THREAD_ID = "default-thread";
